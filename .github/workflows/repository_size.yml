name: 
on: [push]
jobs:
  production-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "::set-output actions/checkout@main
      - run: du -skh --exclude=".git"
